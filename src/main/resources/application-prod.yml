spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  
  datasource:
    hikari:
      maximum-pool-size: 20

logging:
  level:
    root: WARN
    com.wipro.its: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
